#include <stdio.h>

/**
  * main - Count 1 to 10
  * Return: (value = 0)
  */
int main(void)
{
	/*variable int to save*/
	int i;

	/*Loop star from zero to 10*/
	for (i = 0; i < 10; ++i)
	{
		printf("%d", i);
	}
	/*New line*/
	printf("\n");

	return (0);
}
