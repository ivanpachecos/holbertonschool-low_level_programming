#include "main.h"
#include <ctype.h>

/**
  * _isupper - return 1 or 0 if it is uppercase
  * @c: gets a number thar i'm goint to check
  * Return: 1 or 0
  */
int _isupper(int c)
{
	return (isupper(c) ? 1 : 0);
}
