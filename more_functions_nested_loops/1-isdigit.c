#include <ctype.h>

/**
  * _isdigit - checks if is digit or not
  * @c: get data of other file
  * Return: Return the code with iterator the codition
  */
int _isdigit(int c)
{
	return (isdigit(c) ? 1 : 0);

}
